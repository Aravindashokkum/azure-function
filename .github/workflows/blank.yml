name: Trigger on .tfvars Update

on:
  push:
    branches:
      - main  # or the branch you want to monitor
    paths:
      - '*.tfvars'  # This triggers the action on any .tfvars file update

jobs:
  run-on-tfvars-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run your script or command
        run: |
          echo "The .tfvars file has been updated!"
          # You can call your deployment script or any other commands here
